#include "GameEngine.h"

// Constructor definition
GameEngine::GameEngine(const char* title) : title(title) {
    // Initialize any resources if needed
}

// Destructor definition
GameEngine::~GameEngine() {
    // Cleanup resources if necessary
}
